import * as React from "react"
import logo from "./logo.png"

import "./index.css"

const Header = () => (
  <header className="App-header">
    <img src={logo} className="App-logo" alt="logo" />
    <h1 className="App-title">Welcome, adventurer</h1>
  </header>
)

export default Header